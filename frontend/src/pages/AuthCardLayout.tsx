
const AuthCardLayout = ({ children, style, className }) => {
    return <div className={`life_care_project_all_Auth_card ${className}`} style={style}>{children}</div>;
};

export default AuthCardLayout;